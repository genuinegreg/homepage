!!!
html
  head
    != partial("../_partials/head")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")

  body

    .container
        != partial("../_partials/menu")


    .container
        h1.header
            .col-md-8.col-md-offset-4= '<' + title + ' />'

        hr.col-md-8.col-md-offset-4

        != yield
